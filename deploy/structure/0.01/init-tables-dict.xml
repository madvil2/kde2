<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog-ext
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd
                   http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

    <changeSet id="1604084156-10" author="Tikhomirov">
        <sql>
            CREATE TABLE dict_species (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_sex (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_breed_dog (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_breed_cat (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_color (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_ear (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_tail (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_size (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_district (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
        <sql>
            CREATE TABLE dict_fur (
            id BIGSERIAL PRIMARY KEY,
            value TEXT NOT NULL UNIQUE,
            description TEXT
            );
        </sql>
    </changeSet>

</databaseChangeLog>